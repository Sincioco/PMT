# Models Folder Notes

This folder contains simple DTO and input classes used by the API and frontend.

- Keep models as plain classes with clear property names.
- Do not add validation frameworks here unless the app truly needs them.
- Keep properties aligned with the stored procedures and JSON returned to the frontend.
