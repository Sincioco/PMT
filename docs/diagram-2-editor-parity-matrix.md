# Diagram 2 Editor Parity Matrix

This is the Phase 1 authoritative inventory for bringing Diagram 2 editor behavior to Diagram 1 parity. It records the visible Diagram 1 editor surface, the canonical data touched by each behavior, the likely renderer/history impact, and the phase that should own the Diagram 2 implementation.

Phase 1 did not implement any editor features. Phase 2 updated the rows it owns with command, renderer, history, compatibility, performance, and automated-test evidence. Rows that still depend on later-phase behavior remain `Inventoried` or `UI Only` unless the specific behavior is complete.

The post-Phase 1 visual editor parity addendum is incorporated here with the approved Phase 2 correction: Diagram 2 must visually present the familiar Diagram 1 editor by default, while keeping Objects in the right inspector tab/pane as shown in the final Phase 2 screenshots. Toolbar grouping/order, center canvas, right inspector tab names and order, dialogs, context menus, selection affordances, shortcuts, and save/import/export workflows should remain familiar unless Sin explicitly approves a visible difference. This mandate does not permit copying Diagram 1's slow rendering lifecycle; every Diagram 2 behavior still routes through Diagram 2 commands, dirty categories, keyed SVG patches, selective routing, virtualization, and compatibility-safe persistence.

## Status Legend

| Status | Meaning |
| --- | --- |
| Not Started | No Diagram 2 work exists. |
| Inventoried | Diagram 1 behavior and state impact are documented. |
| UI Only | Control exists but operation is not complete. |
| Command Complete | Canonical operation works. |
| Renderer Complete | Incremental renderer integration works. |
| History Complete | Undo/redo works. |
| Compatibility Complete | Diagram 1 to Diagram 2 round-trip works. |
| D1 Parity Complete | Diagram 2 produces the exact Diagram 1 oracle result for the covered behavior. |
| Performance Complete | Regression metrics pass. |
| Manual Approved | Sin approved behavior. |
| Intentionally Deferred | Sin explicitly approved deferral. |

## Phase Ownership

| Phase | Scope |
| --- | --- |
| Phase 2 | Editor foundation, controller, command bus, history, dirty/saved state, and a Diagram 1-familiar shell with toolbar structure, center Diagram 2 canvas, right inspector shell including the Objects tab, and diagnostics moved behind a development/collapsible surface. |
| Phase 3 | Core tools, drawing, selection, resize, format inspector, text editing, keyboard support, core-object clipboard commands, and the selected-object/context commands explicitly completed during Phase 3 closure. |
| Phase 4 | Full object-tree behavior, layers UI, grouping, visibility, rename, templates, and structure workflows not explicitly completed during Phase 3 closure. |
| Phase 5 | Entity, ERD, relationships, manual routes, auto-format, schema generation. |
| Phase 6 | Images, crop, entity annotations, field rectangles, field mapping tables. |
| Phase 7 | Save/import/export, same-document roundtrip, row conflicts, dialogs, UX parity hardening. |
| Phase 8 | Promotion performance hardening, sustained 500/1000 Entity benchmarks, and final promotion-readiness evidence. |

## Phase 2 Completion Update

Phase 2 completed the shared editor foundation and retired the temporary Diagram 2 renderer-development shell as the production editing surface. The implemented scope is intentionally limited to the shell, hosts, cached capability guard, command history, existing movement/nudge behavior, save/export placement, RTE upload-backed annotation save, and shared Diagram document-library/security wiring.

| Parity ID(s) | Phase 2 status | Evidence |
| --- | --- | --- |
| HOST-RTE-001 to HOST-RTE-006 | Compatibility Complete for Phase 2 host launch/save/cancel/menu requirements; later editing tools remain in their owning phases. | `Annotate 2.0` and `Edit Annotation 2.0` launch the shared Diagram 2 editor core beside Diagram 1 actions. Save uses `uploadFile("richtext", file)` and persists the uploaded URL; cancel performs no upload. |
| HOST-DOC-001 to HOST-DOC-002, LIB-001 | Compatibility Complete for shared document-list and host-adapter foundation. | Diagram 2 lists the same backing Diagram documents as Diagram 1 and opens the same IDs through `#/diagram-2/{documentId}`. |
| SHELL-001 to SHELL-006 | UI Only / foundation complete. | Diagram 2 now uses a Diagram 1-familiar toolbar, center canvas, right inspector tab shell with Objects, status region, context menu shell, and collapsible diagnostics. Later-phase tool commands remain disabled where not implemented. |
| TOOL-008, HIST-001 to HIST-002, GEO-001, GEO-005 | History Complete for existing move/nudge operations. | Move and keyboard nudge dispatch command history and incremental renderer updates; undo/redo do not call a complete live `renderer.render(...)`. |
| SAVE-001, IO-001 to IO-003 | Compatibility Complete for existing document save/import/export placement and shared backing records. | Save/import/export continue using shared Diagram contracts and document services, with renderer caches excluded from persisted content. |
| PERF-001 | Performance Complete for Phase 2 local movement/history operations. | Focused browser tests assert unchanged full-render count and keyed object identity for move, nudge, undo, redo, and save. |

## Phase 3 Completion Update

Phase 3 completes the shared core drawing, selection, geometry, inspector, text, clipboard, and context-menu behavior in the top-navigation Diagram 2 host and both RTE Diagram 2 entry paths. The implementation keeps the Phase 2 shell and keyed renderer, uses one shared interaction engine, and does not adopt Diagram 1's full-state snapshot or full-SVG reconstruction path.

| Parity ID(s) | Phase 3 status | Evidence |
| --- | --- | --- |
| TOOL-001 to TOOL-003, TOOL-005 to TOOL-008, KEY-001 to KEY-002 | Performance Complete for the Phase 3 subset. | Select is the default tool and cursor; Pan, Rectangle, Circle, Arrow, Line, Text Box, Rich Text, and Format Painter work through the shared controller. New drawing objects receive sequential type names. Keyboard shortcuts avoid active form and rich-text inputs. Crop and later Entity shortcuts remain intentionally outside Phase 3. |
| SEL-001 to SEL-004 | Performance Complete. | Click, modifier toggle, multi-select, empty-canvas clear, marquee, Select All, locked-object selection, selection handles, and force-mounted selection pass in both renderer harness and production-shell tests. Marquee coordinates use client coordinates corrected by the renderer host bounds. |
| GEO-001 to GEO-006 | Performance Complete for core Phase 3 objects. | Single/multi move, keyboard nudge, side/corner resize, arrow/line endpoints, minimum sizes, Grid/Snap, preview cancellation, pointer-capture loss, virtualization-sector crossing, and one-command gesture commits are covered without a routine full render. Relationship previews replace stale routes while moving and terminate at Entity borders. |
| STR-001, STR-004, STR-007 | Performance Complete for the explicitly requested Phase 3 context subset. | Duplicate, lock/unlock, To Front, To Back, Forward, and Backward are undoable commands. Layer ordering preserves multi-selection order and reorders mounted SVG nodes without full Objects-tree reorder. Groups, visibility, rename, tree drag/drop, and a full Layers UI remain Phase 4. |
| STYLE-001 to STYLE-006 | Performance Complete for supported Phase 3 objects. | Fill, outline, opacity, stroke width, arrow size, text styles/alignment, mixed values, split-button color memory, Text Box editing, and the full shared Rich Text editor work in both hosts. Style-only changes patch selected objects and reroute zero relationships. |
| CLIP-001 to CLIP-003 | Compatibility and History Complete for core Phase 3 objects. | Copy, Paste, Duplicate, repeated offsets, ID remap, browser-tab fallback, Diagram 1 to Diagram 2, Diagram 2 to Diagram 1, top-navigation, and RTE paths use `pmt-diagram-selection` v1 without renderer state. |
| CTX-001 | Performance Complete for the Phase 3 subset. | Object context actions use the same controller commands as keyboard/toolbar behavior: arrange, lock, copy, paste, duplicate, delete, Copy as SVG, and Copy as Image. Prohibited Crop, Group, Entity, relationship, and full tree actions are absent. |
| CTX-002, IO-002 to IO-003 | Compatibility Complete for the added canvas-copy path. | Read-only and Edit-mode canvas right-click menus offer Copy as SVG and Copy as PNG and reuse the existing SVG/PNG options dialogs and clipboard helpers. Existing left-tree download behavior remains unchanged. |
| HIST-001 to HIST-002, PERF-001, PERF-003 | Performance Complete for Phase 3. | Commands have inverse operations; gestures create one history entry; style changes coalesce; keyed node identity is retained. The 238-object/624-relationship stress gate stays at one full render with selection, marquee, drag, resize, and style operations under their budgets. |
| IMG-001 | Compatibility Complete for native clipboard-image paste; remaining image scope deferred. | Native clipboard-image paste into Diagram 2 Edit mode uploads through the existing Rich Text upload path and creates one editable `embedded-image` object through command history. Full image upload/drop, asset management, and related image-library behavior remain Phase 6. |
| IMG-002 | Intentionally Deferred to Phase 6. | Crop inset/radius/reset/permanent crop remains excluded from Phase 3. |
| STR-002 to STR-003, STR-005 to STR-006, STR-008 to STR-009, TPL-001 to TPL-005 | Intentionally Deferred from Phase 3 to Phase 4. | Groups, full Layers/Object-tree behavior, visibility, rename, tree search/reorder, and Templates were expressly excluded from Phase 3 and are recorded in the Phase 4 update below. |
| ERD-001 to ERD-015 | Completed through Phase 5 closure. | Entity creation/editing, visible field rows, relationships, manual route controls, Auto Format - Compact, and live PMT schema generation are implemented for Diagram 2 top-navigation and RTE hosts. |
| MAP-001 to MAP-008 | Intentionally Deferred to Phase 6. | Entity annotations, Field Rectangles, Field Mapping, and Field Mapping Tables remain excluded from Phase 3. Existing canonical compatibility is preserved but no Phase 3 UI was added. |

## Phase 4 Completion Update

Phase 4 completes the authorized structure, Objects tree, group, Layers, template, and drawing-default scope through the shared Diagram 2 editor controller used by top-navigation Diagram 2 and both Rich Text `Annotate 2.0` / `Edit Annotation 2.0` hosts. No Phase 5 Entity/relationship/manual-route/Auto Format work and no Phase 6 Crop/Field Mapping authoring work is included.

| Parity ID(s) | Phase 4 status | Evidence |
| --- | --- | --- |
| STR-002 to STR-003 | Performance Complete. | Group and Ungroup are command-based, undoable, keyboard-addressable through Ctrl+G / Ctrl+Shift+G, and preserve keyed SVG node identity through renderer-local structure patches. |
| STR-005 to STR-009, STR-007 | Performance Complete. | Objects tree rows now support group-aware selection, visibility, rename, search/filter, drag/drop reorder, root reorder, and a Layers toolbar. Group-aware z-order keeps grouped members compact and avoids routine full renders. |
| TPL-001 to TPL-005 | Compatibility Complete / Performance Complete for Phase 4. | Diagram 2 reuses the shared Diagram template endpoints/codecs, can save/apply/format/upload/download/delete/reorder/restore templates, and persists arrow/rectangle drawing defaults outside canonical document serialization. |
| CLIP-001 to CLIP-003 | Compatibility Complete with Phase 4 group metadata. | Selection clipboard paste/duplicate preserves group names and visibility metadata, remaps advanced structured references, and continues to use clean `pmt-diagram-selection` v1 packages without renderer state. |
| KEY-002, CTX-001 | Performance Complete for Phase 4 additions. | Group/Ungroup shortcuts and context/menu controls route through the same shared controller commands as the toolbar/Object tree. |
| IMG-001 to IMG-002 | Status clarified. | Native clipboard-image paste is implemented. Full image upload/drop, asset management, and Crop remain Phase 6. |
| PERF-001, PERF-003 | Performance Complete for Phase 4 closure evidence. | The final 1,000-object fixture covers Objects-tree projection/search, canvas virtualization, tree-to-canvas selection, object-tree focus, visibility, rename, lock, reorder, undo, and redo with zero routine full-render delta. |

Closure audit: Diagram 1 exposes text alignment controls, but no object alignment, distribution, same-width, same-height, or same-size command surface was found. No Diagram 2 object alignment/distribution/same-size rows or implementation were added for Phase 4. Text alignment remains covered by `STYLE-005`.

## Phase 5 Completion Update

Phase 5 adds shared Diagram 2 Entity, ERD relationship, manual route, and Auto Format - Compact behavior through the same controller used by the top-navigation Diagram 2 host and both Rich Text `Annotate 2.0` / `Edit Annotation 2.0` hosts. The final implementation commit is `237b229aa208dde69b4d29ecf07eb1335deac083`. The implementation keeps the Phase 2-4 keyed renderer architecture, reuses renderer-neutral Diagram 1 parsing/routing helpers, and does not add Phase 6 image, Crop, Field Rectangle authoring, Field Mapping authoring, or Phase 7 workflow-promotion behavior.

| Parity ID(s) | Phase 5 status | Evidence |
| --- | --- | --- |
| ERD-001 to ERD-004 | Compatibility Complete / History Complete. | Entity creation is enabled from the shared toolbar and `E` shortcut. The shared Entity dialog uses the Diagram 1 parser for SQL Server `CREATE TABLE` and field-list input, preserves compatible canonical Entity properties, supports schema/name/header edits through command history, and exposes Reset Scale for the selected unlocked Entity. |
| ERD-005 to ERD-009 | History Complete / Browser Complete. | Field add/update/remove/reorder command plans cover field name, data type, nullable, identity, primary key, foreign key, important flag, duplicate-name suffixing, reference dropdowns, automatic Entity sizing, and relationship cleanup. The visible inspector field editor is covered in top-navigation and RTE browser tests. |
| ERD-010 to ERD-013 | Performance Complete / Browser Complete. | Relationship creation/deletion, canvas and Objects-tree selection, D1-style route joints, segment/joint drag, double-click joint creation, right-click joint removal, keyboard nudge, clear-to-automatic, self relationships, cardinality symbols, global/individual style, overlap, and one-commit route preview are shared across top-navigation and RTE hosts. Entity drag previews use D1 route geometry. |
| ERD-014 | D1 Parity Complete / History Complete / Renderer Complete / Hardening Complete. | `Entity -> Auto Format -> Compact` calls the same D1 state operation with the exact selected root and D1 options. Size-based summary decisions, skipped route adjustment, the generic grid candidate, and the separate D2 score veto were removed. Exactly one selected Entity and all Entities unlocked are required. D1, D2 inline, and D2 worker results match with zero Entity-position, automatic-route-point, or manual-route mismatches across all required fixtures. Cancel/stale/no-change create no history; success creates one command with exact undo/redo. |
| ERD-015 | Compatibility Complete / Browser Complete. | Diagram 2 has a live `Generate PMT Database Schema` button that fetches `/api/diagram/pmt-database-schema`, builds the compatible Entity graph, uploads SVG through the existing Rich Text upload path, and creates a Documentation-backed Diagram. Existing PMT schema fixture/import evidence remains green with 88 canonical objects and 78 relationships. |
| HOST-RTE-001 to HOST-RTE-006 | Compatibility Complete for Phase 5 Entity/relationship editing. | `Annotate 2.0` and `Edit Annotation 2.0` reuse the same Entity dialog, relationship dialog, inspector controls, controller commands, manual route handles, and Compact command as the top-navigation host. |
| PERF-002 to PERF-003 | Performance Complete for the Phase 5 functional gates. | Browser evidence covers selective 232/624 routing plus actual 500- and 1,000-Entity selection, marquee, field edit, move, relationship style, manual route, low detail, virtualization, worker start, cancellation, and cleanup with one initial full render and no measured routine full-render increase. Phase 8 still owns sustained promotion timing. |

## Phase 6 Completion Update

Phase 6 completes the authorized image, Crop, Entity Annotation, Field Rectangle, field mapping, Field Mapping Table, layering, and read-only mapping scope through the same controller, renderer, shell, and dual host boundary established in Phases 2-5. The implementation adds keyed image resources and mapping indexes without adding a second asset store, file format, clipboard format, template store, document type, or database change.

| Parity ID(s) | Phase 6 status | Evidence |
| --- | --- | --- |
| IMG-001 to IMG-002, TOOL-004, OBJ-007 | D1 Parity Complete / Compatibility Complete / History Complete / Renderer Complete. | Top-navigation and RTE hosts share file upload, clipboard paste, image drop, all nine Crop numeric controls, pointer/keyboard handles, reversible reset, confirmed permanent crop, missing-image fallback, copy/template/save/export compatibility, and keyed image decode/release behavior. Crop numeric input uses one 500 ms trailing scheduler and one command per burst; D1's separate 3,000 ms selection-quiet behavior is renderer-local. D1's durable opacity rule is preserved: vector opacity does not alter source or embedded images. |
| MAP-001 to MAP-004, OBJ-009 | Compatibility Complete / History Complete / Renderer Complete. | Entity annotations and arrows, virtual-Entity Field Rectangles, connection sides, database Entity/field mapping, relationship type, automatic Field Rectangle naming, object-tree projection, copy/paste, template, undo, redo, and round-trip behavior use compatible canonical metadata. |
| MAP-005 to MAP-006, MAP-008 | Performance Complete / Browser Complete. | Editor and read-only hover/click/double-click/keyboard interactions patch keyed highlight targets and mapping routes without canonical mutation, full render, broad DOM queries, unrelated object scans, or unrelated Entity rerouting. |
| MAP-007, OBJ-010 | Compatibility Complete / Renderer Complete for Phase 6 table scope. | Field Mapping Tables use stable mapping row keys, synchronize only affected image/table rows, preserve D1 table colors/styles, and participate in standard Diagram SVG/PNG/PMT export. Dedicated Excel/CSV workflow hardening remains Phase 7 as originally assigned. |
| HOST-RTE-001 to HOST-RTE-006 | Compatibility Complete for Phase 6 image/crop/mapping editing. | `Annotate 2.0` and `Edit Annotation 2.0` reuse the same Phase 6 host adapter, shell controls, commands, mapping dialogs, renderer planes, and cleanup path as top-navigation Diagram 2. Save updates the same selected RTE image; cancel uploads nothing and releases the renderer/controller/image resources. |
| PERF-001 to PERF-003 | Performance Complete for Phase 6 functional gates. | Crop preview patches only the selected image/overlay, mapping hover is view-only, and one mapping edit visits zero unrelated objects in the 500- and 1,000-object gates. Compact remains 27/27 with zero D1 mismatches and zero Compact planning full renders. |

### Phase 6 Closure Update

The closure pass makes Diagram 1 executable behavior the Field Mapping Table oracle and closes the shared navigation and lifecycle gaps without starting Phase 7 or Phase 8.

| Parity ID(s) | Closure status | Evidence |
| --- | --- | --- |
| MAP-005, MAP-007 to MAP-008 | D1 Parity Complete / Performance Complete. | D1 and D2 now use the same table layout and attention-arrow geometry helpers. UI and Database cells retain distinct metadata and activation targets. Single click pins/highlights without recentering; double-click focuses the Field Rectangle or exact database field respectively; Enter/Space activate; Escape/outside click clear. Two focus-ring-blue arrows expire after three seconds while the configurable yellow mapping highlight remains independent. The canonical comparison fixture reports zero table mismatches and zero arrow geometry errors outside 0.5 diagram units. |
| HOST-RTE-001 to HOST-RTE-006 | Compatibility Complete / Lifecycle Complete for Phase 6. | Physical D1 to D2 to D1 and D2 to D1 to D2 RTE round trips preserve reversible crop, Field Rectangles, many-to-one mappings, table colors/row styles, Entity annotations, and editable metadata. Ten mixed Annotate-cancel, Annotate-save, and Edit-save cycles alternate with top-navigation sessions and close renderers, controllers, hosts, image resources, workers, observers, timers, and global references. |
| IMG-001 to IMG-002, TOOL-004 | D1 Parity Complete / Performance Complete / Browser Complete. | The shared D1/D2 fixture proves matching effective Crop bounds and corner radii, full-boundary activation, existing-crop Cancel/Remove/Permanent choices, hidden-crop restoration, insets, uniform/independent radii, Reset, Undo/Redo, visibility, Save/Reopen, and both hosts. Twenty real-input events without Tab collapse after 500 ms to one image patch, one Crop-overlay patch, and one history command with zero unrelated patches, reroutes, full renders, or decodes. Ordinary selection chrome is suppressed locally while Crop handles remain visible and returns after D1's 3,000 ms quiet period. Real image-file drag/drop and image clipboard events each upload once, add one canonical image, create one history entry, avoid a full render, and survive save/reopen. Permanent crop replaces the source, normalizes crop metadata, releases the old resource, decodes the replacement once, persists after reopen, and applies D1's irreversible-history rule. |
| SEL-001 to SEL-004, PERF-001 | Performance Complete. | D2 marquee movement updates only the marquee boundary. Object hit-testing and selection changes occur once at pointer-up; blank-canvas completion clears the prior selection. |
| PERF-001 to PERF-003 | Performance Complete for the closure regressions. | D2 starts from the saved zoom. Low detail simplifies Entity contents only; relationship paths retain the same manual or fast orthogonal geometry below and above 25% zoom. The 29-Entity/78-relationship focused fixture reports zero path mismatches between 20% and 50%, and document 31 reports zero mismatches between 20% and 35% in both read-only and edit mode. Compact remains visibly routed at low zoom. Every measured interaction remains below 500 ms. |
| ERD-010 to ERD-013, HIST-001 to HIST-002, PERF-001 to PERF-003 | Performance Complete / History Complete / Dual-host Complete. | Route-joint release now passes the final preview geometry to a dedicated delta command and patches one indexed FK connector. Across 20 post-warm-up drags each on 2/1, 96/257, 232/624, 500/1, and 1,000/1 fixtures, settled p95 is 13.1, 11.2, 20.3, 20.2, and 45.8 ms; visible-path p95 is 0.8, 0.7, 1.4, 7.0, and 15.7 ms. Each drag adds one history entry, visits/patches one object and FK, considers one relationship, reroutes zero, rebuilds zero indexes, and causes zero dirty flushes or full renders. Exact Undo/Redo, cancellation, first-manual-route transition, top-navigation, `Annotate 2.0`, and `Edit Annotation 2.0` are covered. |

Shared Documentation, Diagram 1, and Diagram 2 TreeNav filtering now exposes the Documentation dialog's full Search, Project, Sprint, Visibility, Group, Layout, Sort, Creator, and Last Editor behavior. Tree selection preserves the exact scroll owner, focus, selected row, expansion state, and horizontal/vertical offsets within two CSS pixels; an external route reveals a hidden selection only when necessary.

## Visual Parity Control Register

This register adds the addendum-required visual metadata for every inventory row already captured below. Rows that group multiple controls keep the same grouped scope as the matrix and must be split when a later phase implements the controls individually.

| ID(s) | Visual source or builder | CSS source | Action name(s) | Enabled/disabled rules | Selection rules | Diagram 2 command owner | Expected phase |
| --- | --- | --- | --- | --- | --- | --- | --- |
| HOST-RTE-001 to HOST-RTE-006 | Rich-text image context menu built by `showRichTextImageMenu`; Diagram 1 launch through `annotateRichTextImage`; future Diagram 2 launch must use the same familiar annotation editor shell minus document-library UI | Rich image menu/dropdown styles in existing rich-text CSS and shared dropdown styles; annotation editor visuals from `image-annotation.css`; Diagram 2 host styling in `diagram2.css` only where renderer-specific | Existing `annotate`; future `annotate2`; labels should pair existing `Annotate` / current `Edit Annotation` with `Annotate 2.0` / `Edit Annotation 2.0` unless Sin explicitly approves the addendum's alternate `Edit Annotate` wording | Show only for supported selected RTE images; existing Diagram 1 action stays enabled; Diagram 2 action must not appear as functional until the RTE host adapter can open, save, cancel, and clean up correctly | Requires selected image inside `.rich-editor`; annotated state currently detected by `data-pmt-annotation-version`; save-back targets the same selected image | `diagram2-rte-host-adapter`, shared `diagram2-editor-controller`, shared renderer | Phase 2 architecture and menu integration; full editing parity follows owning feature phases |
| HOST-DOC-001 to HOST-DOC-006 | Top-navigation Diagram 2 screen and document library; future document host embeds the same Diagram 2 editor core | `diagram2.css`, shared section/header/dialog styles, Diagram 1 editor visual references from `image-annotation.css` | `#/diagram-2`, `#/diagram-2/{documentId}`, document open/create/save/metadata/import/export | Enabled by route, selected document, permissions, visibility, project/sprint metadata, row version, and document load state | Active Diagram document selection; object selection lives inside shared editor core | `diagram2-document-host-adapter`, shared `diagram2-editor-controller`, shared renderer | Existing route remains; Phase 2 defines adapter seam; Phase 7 hardens full document workflows |
| SHELL-001 to SHELL-006 | `openImageAnnotationDialog`, `annotationDialogHtml`, dialog header/footer, `.image-annotation-main`, inspector splitter | `wwwroot/css/components/image-annotation.css`; future editor-mode shell in `wwwroot/css/features/diagram2.css` may reuse renderer-neutral classes/tokens | `editDiagram`, `createNewDiagram`, `apply`, `cancel`, maximize/restore, pane resize | Enabled by document edit permission and document load state; crop tab conditional; inspector resize disabled while shell inactive | No selection required for opening; shell surfaces selection-aware controls downstream | `diagram2-editor-controller`, `diagram2-editor-shell`, `diagram2-editor-history` | Phase 2 |
| LIB-001 to LIB-003 | Diagram screen `sectionHead`, card/tree library, filters dialog/search rows | `wwwroot/css/features/diagram.css`; current Diagram 2 library CSS in `diagram2.css` | `loadDiagramDocuments`, `set-diagram-view`, `toggle-diagram-tree-pane`, `openDiagramFiltersDialog`, filter actions | Library actions enabled after state load; filters enabled when metadata options exist | Document selection chooses active backing doc; object selection not relevant | Existing Diagram 2 document shell, later `diagram2-domain-adapter` | Phase 7 for full dialog parity; Phase 2 keeps shell placement compatible |
| DOC-001 to DOC-005 | Diagram header/page/tree document actions and tree context menu | `diagram.css`, shared button/dropdown styles, `image-annotation.css` for editor entry | `createNewDiagram`, `editDiagramInfo`, `duplicateDiagram`, `deleteItem`, public link, tree downloads | Permission and row-version dependent; delete/duplicate require a document; edit requires write access | Active document required; object selection not relevant | `diagram2-document-commands`, `diagram2-domain-adapter` | Phase 7 |
| VIEW-001 to VIEW-002 | Read-only viewer controls and read-only context menu | `diagram.css`; Diagram 2 canvas/renderer classes in `diagram2.css`; shared dropdown menu styles | `zoom out`, `zoom select`, `zoom in`, `fit`, toggle relationships, toggle mapping, copy SVG/PNG | Enabled when a document and parsed SVG/canonical state are loaded; relationship/mapping toggles enabled only when data exists | Read-only field/relationship selection rules apply; no edit selection required | Existing renderer viewport API plus `diagram2-view-commands` | Phase 7, with existing baseline preserved in Phase 2 |
| IO-001 to IO-005 | Header/tree import/export buttons, hidden file input, context menu export rows | `diagram.css`, shared dropdown/menu styles | `importPmtDiagramFile`, `exportPmtDiagram`, SVG download, PNG copy/export, portable asset handling | Import enabled with create permission; export enabled with active document; PNG/SVG require buildable canonical state | Export document actions do not require object selection; selection copy handled separately | `diagram2-io-commands`, shared `diagram-contracts`, `diagram2-domain-adapter` | Phase 7 |
| TOOL-001 to TOOL-008 | Main Diagram 1 editor toolbar buttons from `annotationDialogHtml`, `annotationToolbarToolButtonHtml`, `annotationToolbarActionButtonHtml` | `image-annotation.css` toolbar classes and icon button styles; Diagram 2 may share renderer-neutral toolbar styles | Select, Pan, Format Painter, Crop, Grid, Snap, Delete, Undo, Redo | Crop disabled without image/crop target or when locked; Delete disabled with empty selection or protected relationship state; Undo/Redo disabled at stack ends; Grid/Snap available in edit session | Select/Pan/Grid/Snap need no selection; Format Painter needs style source/apply target; Delete needs valid selection | `diagram2-editor-toolbar`, `diagram2-editor-controller`, `diagram2-editor-commands`, `diagram2-editor-history` | Phase 2 shell; Phase 3 behavior; Phase 6 crop |
| OBJ-001 to OBJ-010 | Toolbar tool buttons and drawing/default controls in `annotationDialogHtml` | `image-annotation.css` toolbar, canvas cursor, object visual styles; renderer visuals in `diagram2.css` | Add rectangle, circle, arrow, line, text box, rich text, image, entity, field rectangle, field mapping table | Drawing tools enabled in editable session; image requires asset support; mapping/entity tools require compatible state; buttons may be absent or clearly disabled before owning phase | New object usually becomes selected; field rectangle/mapping depends on selected or target entity/field where applicable | `diagram2-editor-commands`, `diagram2-editor-interactions`, `diagram2-domain-adapter` | Phase 3 for core shapes/text; Phase 5 entity; Phase 6 image/mapping |
| SEL-001 to SEL-004 | Diagram 1 selection outlines, selected object classes, marquee overlay, keyboard selection handling | `image-annotation.css` selection/handle/workspace classes; `diagram2.css` selected renderer object classes | Click select, modifier multi-select, marquee, Select All | Enabled in edit session; locked/hidden objects follow Diagram 1 hit/select rules | Selection rules are the feature: empty, one object, multiple objects, relationship, entity, field rectangle, crop target | `diagram2-selection-commands`, `diagram2-editor-interactions`, renderer selection API | Phase 3 |
| GEO-001 to GEO-006 | Diagram 1 drag/resize handles, cursor affordances, arrow endpoint helpers | `image-annotation.css` handle/cursor/workspace classes; renderer preview classes in `diagram2.css` | Drag, multi-drag, corner/side resize, snap, keyboard nudge, arrow endpoint fit | Enabled when selected objects are editable and unlocked; resize disabled for unsupported object kinds; snap optional | Requires valid editable selection; multi-drag requires multiple selected objects | `diagram2-geometry-commands`, `diagram2-editor-interactions`, renderer preview APIs | Phase 3 |
| STR-001 to STR-009 | Objects tab, object tree rows/actions, context/action buttons | `image-annotation.css` object tree, row actions, lock/visibility/crop status styles | Duplicate, group, ungroup, lock, visibility, rename, bring/send, tree search, tree reorder | Group requires 2+ compatible unlocked objects; ungroup requires group; layer/lock/visibility/rename require editable object; tree search always enabled | Most commands require selected object(s); tree search no selection; tree reorder requires draggable row/selection | `diagram2-structure-commands`, `diagram2-editor-objects-tree` | Phase 4 |
| STYLE-001 to STYLE-006 | Format inspector panel in `annotationDialogHtml` | `image-annotation.css` inspector grids/labels/inputs; shared form/color picker styles | Fill, outline, opacity, line width, arrow heads, symbols, text style, text content | Enabled for selected object(s) that support each property; mixed values must show appropriately; text controls disabled for non-text objects | Requires compatible selection except defaults handled by Template/default controls | `diagram2-style-commands`, `diagram2-editor-inspector` | Phase 3; relationship symbols continue in Phase 5 |
| ERD-001 to ERD-015 | Entity toolbar button and Entity inspector panel | `image-annotation.css` Entity panel, field rows, object tree/entity row styles; renderer entity styles in `diagram2.css` | Create entity, parse SQL/fields, edit schema/name, add/remove/reorder fields, PK/FK flags, collapse, display toggles, routing options, relationships, manual routes, auto-format, generate PMT schema | Entity controls enabled only for entity/field-rectangle compatible selection and edit permission; generator enabled when schema loader succeeds; destructive route clears require confirmation if Diagram 1 does | Entity selected for most inspector controls; relationship selected for relationship-specific controls; some global toggles apply to all entities | `diagram2-entity-commands`, `diagram2-relationship-commands`, `diagram2-layout-commands` | Phase 5 |
| MAP-001 to MAP-008 | Mapping inspector tab, entity annotation controls, field rectangle controls, read-only mapping overlays | `image-annotation.css` Mapping panel and field mapping table styles; renderer overlay styles in `diagram2.css` | Entity annotations, field rectangle edit, map UI to DB field, many-to-one mapping, hover/highlight, generate mapping, mapping table colors/export, read-only interactions | Mapping controls enabled when mapping-capable entities/field rectangles/tables exist; export enabled when table rows exist | Requires selected entity, field rectangle, mapping table, or hovered/read-only mapping target depending on control | `diagram2-mapping-commands`, `diagram2-field-rectangle-commands`, `diagram2-export-commands` | Phase 6; read-only parity hardening Phase 7 |
| IMG-001 to IMG-002 | Image paste/upload flow and Crop inspector panel | `image-annotation.css` Crop panel, crop handles, object tree crop status; renderer image/crop styles in `diagram2.css` | Insert image, crop inset/radius/reset/permanent crop | Insert enabled with edit/asset support; crop controls enabled only when selected image or reversible crop target exists; permanent crop gated by confirmation if Diagram 1 does | Crop requires selected crop-capable image/object | `diagram2-image-commands`, `diagram2-crop-commands` | Phase 6 |
| TPL-001 to TPL-005 | Template inspector panel and default-style controls | `image-annotation.css` Template panel, toolbar action styles, shared form controls | Apply, save selection as template, upload/download/delete, restore defaults, use/reset selected arrow/rectangle defaults | Apply enabled when template exists; save requires selection; upload requires valid file; delete/restore permission follows existing template endpoint behavior | Save template requires selection; apply normally selects created objects; defaults require selected compatible object | `diagram2-template-commands`, shared template endpoints | Phase 4 |
| CLIP-001 to CLIP-003 | Toolbar/context/keyboard clipboard affordances | `image-annotation.css` toolbar/context styles; shared clipboard contract has no visual CSS | Copy, paste, D1 to D2 clipboard, D2 to D1 clipboard | Copy enabled with selection; paste enabled when clipboard parses and edit session active | Copy requires selected objects; paste creates and selects remapped objects | `diagram2-clipboard-commands`, shared `pmt-diagram-selection` v1 codec | Phase 4 |
| HIST-001 to HIST-002 | Undo/Redo toolbar buttons and keyboard state | `image-annotation.css` toolbar disabled/active styles | Undo, redo, coalesced transaction begin/commit | Undo disabled at baseline; redo disabled at stack head; transaction coalescing active for gestures/typing/sliders | Selection restored according to command metadata | `diagram2-editor-history`, `diagram2-editor-commands` | Phase 2 |
| KEY-001 to KEY-002 | Editor keydown handler and toolbar tooltip shortcut labels | No dedicated CSS beyond toolbar labels/tooltips and focus styles | V/H/C/R/O/A/L/T/Y/E, Ctrl+Z/Y/C/V/D/A/G/Shift+G, Delete, arrows, Escape | Disabled while typing in inputs where Diagram 1 ignores shortcuts; permission/selection gates mirror command availability | Per command; Escape clears selection/gesture; arrows require selection | `diagram2-keyboard-controller`, `diagram2-editor-controller` | Phase 3 and Phase 4, with shell router in Phase 2 |
| CTX-001 to CTX-002 | `annotationContextMenuHtml`, Diagram tree context menu | `image-annotation.css` context menu styles, shared dropdown menu styles, `diagram.css` tree menu styles | Object context actions; tree edit info/edit/duplicate/link/download/export/delete | Menu rows disabled/hidden by same permission, selection, lock, object kind, and document availability rules as toolbar/tree | Object context requires context target/selection; tree context requires document row | `diagram2-context-menu-controller`, command modules by action | Phase 4 object context; Phase 7 document context |
| SAVE-001 to SAVE-003 | Footer Apply/Cancel, Save button placement, save conflict dialog | `image-annotation.css` footer/dialog styles, shared modal/dialog CSS | Save/apply, save-as-new conflict, cancel/close | Save enabled when dirty and editable; conflict options enabled from row-version response; cancel always available but prompts if Diagram 1 does | Save applies whole canonical document, not selection-specific | `diagram2-save-commands`, shared document save service | Phase 7; Phase 2 keeps Save placement |
| PERF-001 to PERF-003 | Development diagnostics surface only; not production toolbar-first UI | `diagram2.css` diagnostics styles, future collapsible/dev-only panel | Full-render count, routing counts, viewport halo/low-detail metrics | Diagnostics hidden/collapsed outside dev surface; never required for normal editing | No selection required except selected-object diagnostics | `diagram2-performance-contracts`, renderer diagnostics API | Phase 2 creates dev toggle; Phase 8 hardens gates |

## Matrix

| ID | Category | Diagram 1 feature | UI location | Action/handler | Canonical reads/writes | Diagram 2 strategy | Dirty category | Routing impact | History command | D1 to D2 | D2 to D1 | Performance test | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| HOST-RTE-001 | RTE host | RTE image annotation launch | Rich-text image context menu | `handleRichTextImageClick`, `showRichTextImageMenu`, existing action `annotate` | Reads selected RTE image, `src`, `data-pmt-annotation-version`, `data-pmt-annotation-source`; writes nothing on menu open | Phase 2 adds sibling Diagram 2 action only when the shared RTE host adapter can launch the shared editor core | `none`, `lifecycle` | None on menu open | No canonical history | D1 command remains available | D2 command must not replace D1 | Menu label test, no editor/render work on open | Inventoried | Current code label is `Edit Annotation`, not `Edit Annotate`; preserve existing D1 label unless Sin approves renaming. |
| HOST-RTE-002 | RTE host | RTE new annotation through Diagram 2 | Rich-text image context menu beside `Annotate` | Future action `annotate2`; future adapter e.g. `openDiagram2RteAnnotationHost` | Reads selected image source; writes canonical annotation only after Save | Use shared Diagram 2 editor core with mode `rte-annotation`; no route change or standalone Diagram doc by default | `document`, `asset`, `lifecycle` | Only routes inside edited annotation | Save/cancel boundary plus per-command history inside editor | Opens D1 image source/state | Diagram 1 can reopen supported saved annotation | RTE new annotation, cancel, cleanup, no full live render | Inventoried | Do not modify RTE image on open or during cancel. |
| HOST-RTE-003 | RTE host | RTE edit existing annotation through Diagram 2 | Rich-text image context menu beside current edit label | Future action `annotate2` when annotated; existing action `annotate` remains Diagram 1 | Reads existing SVG/canonical metadata from selected image; writes back same image on Save | Shared Diagram 2 editor parses shared canonical annotation; renderer-only state excluded from save | `document`, `lifecycle` | Existing relationships rendered through Diagram 2 renderer | Save/cancel boundary plus command history | D1-created annotation opens in D2 | D2 save reopens in D1 where features supported | D1 to D2 to D1 RTE roundtrip | Inventoried | Selected image must not be flattened to bitmap. |
| HOST-RTE-004 | RTE host | RTE save-back | Diagram 2 RTE annotation modal/dialog footer | Existing D1 save-back in `annotateRichTextImage`; future Diagram 2 `saveAdapter` | Writes uploaded full-detail SVG URL to selected image `src`; preserves `data-pmt-annotation-source` and `data-pmt-annotation-version` | Host adapter owns upload and same-image update; editor core returns full-detail SVG/canonical state | `document`, `asset` | Save/export path only, not live rendering | Save clears dirty state after successful adapter completion | D1 metadata preserved | D2 metadata compatible | Save-back smoke and metadata assertions | Inventoried | Restore RTE focus/selection after save. |
| HOST-RTE-005 | RTE host | RTE cancel/close cleanup | Diagram 2 RTE annotation modal/dialog | Existing cancel returns no result; future `cancelAdapter`/`closeAdapter` | Writes nothing to RTE content on cancel | Dispose editor/renderer/listeners/object URLs and restore RTE focus/selection | `lifecycle` | Cleanup only | No canonical history | Original image unchanged | Original image unchanged | Cancel and ten-cycle cleanup | Inventoried | Opening alone must not mutate content. |
| HOST-RTE-006 | RTE host | Side-by-side labels | Rich-text image context menu | Existing `Annotate` / `Edit Annotation`; future `Annotate 2.0` / `Edit Annotation 2.0` | Reads annotation metadata only | Add paired labels in the same context-menu group once functional | `none` | None | No canonical history | D1 commands preserved | D2 commands adjacent | Label test for unannotated and annotated images | Inventoried | Addendum says `Edit Annotate`; current app says `Edit Annotation`. This row preserves current app wording. |
| HOST-DOC-001 | Document host | Top-navigation Diagram 2 launch | PMT top navigation and `#/diagram-2` routes | `createDiagram2Feature`, route `Diagram 2`, document selection actions | Reads Diagram document library and selected document state; writes preferences/route on selection | Host adapter embeds shared Diagram 2 editor core for document mode | `document`, `lifecycle` | Initial render and selected doc changes only | Document session boundary | D1 docs open in D2 | D2 saves reopen in D1 | Route open/close cleanup | Inventoried | Existing route remains `Diagram 2`. |
| HOST-DOC-002 | Document host | Shared core, host-specific document responsibilities | Top-navigation Diagram 2 document editor | Future document host adapter | Reads/writes selected Diagram document, metadata, row version, project/sprint, visibility | Inject document save/cancel/close/notify/permissions callbacks into shared editor core | `document` plus per-command categories | Per command | Per command | Same backing docs | Same backing docs | D1-D2-D1 document roundtrip | Inventoried | Do not create second Diagram document model or force RTE concerns into document host. |
| SHELL-001 | Shell | Editor open/close lifecycle | Diagram card/tree `Edit Diagram`; new diagram flow | `editDiagram`, `createNewDiagram`, `openImageAnnotationDialog` | Reads document SVG/state, template library, schema; writes saved body on apply | Phase 2 creates Diagram 2 editor controller and modal/embedded shell over same document loader | `none`, `lifecycle` | Must not call full render except initial open/reset | `open-session`, `close-session` non-persisting | Reuse parsed state from shared document | Save compatible SVG only | Open/close 10 cycles, no stale DOM/maps | Inventoried | Current Diagram 2 has viewer shell and a small edit path, not full editor shell parity. |
| SHELL-002 | Shell | Maximized and embedded modes | Dialog header maximize/restore; embedded options | Dialog local state `maximized`, `embedded`, `fullscreen` | Reads viewport/canvas bounds; writes UI-only layout state | Phase 2 keeps shell state outside canonical state | `viewport` | None | UI history not persisted | No file effect | No file effect | Resize/open/close with stable selection and transform | Inventoried | Diagram 1 supports both modes through one large component. |
| SHELL-003 | Shell | Toolbar groups and command buttons | Main editor toolbar | `data-annotation-tool`, `data-annotation-action`, zoom actions | Reads selection, tool, zoom, grid/snap, dirty state; writes active tool/options | Phase 2 shell defines command slots; Phase 3-7 fill command implementations | Per command | Per command | Per command | N/A | N/A | Toolbar must not resize canvas or force render | Inventoried | Buttons include Select, Pan, Format Painter, Crop, Rectangle, Circle, Arrow, Line, Text Box, Rich Text, Entity, Field Rectangle, Mapping Table, Undo, Redo, Delete, Grid, Snap, Zoom, Fit, Pane, Maximize, Cancel, Save. |
| SHELL-004 | Shell | Inspector tabs | Right pane tabs: Format, Template, Objects in Phase 2; Crop, Mapping, Entity, and other future tabs appear only when behavior is functional or an honest disabled control is specifically required | `activeInspectorTab`, tab buttons | Reads selection/object capabilities; writes active tab UI state | Phase 2 shell owns tab state; later phases add panels | `none` | None | No canonical history for tab switching | No file effect | No file effect | Tab switch under 16.7 ms target and no render | Inventoried | The approved Phase 2 screenshots place Objects in the right inspector tab/pane. |
| SHELL-005 | Shell | Status/save indicator | Footer/header status area | `status`, `historyIndex`, dirty/saved tracking | Reads dirty state, save errors, row version; writes UI status | Phase 2 command bus publishes status | `none` | None | No canonical history | No file effect | No file effect | Dirty indicator updates without renderer refresh | Inventoried | Current Diagram 2 has dirty/saved labels but no command model. |
| SHELL-006 | Shell | Responsive editor layout | Dialog/shell CSS and pane toggles | `toggle inspector`, resize observer, pane width state | Reads container size, inspector width; writes UI-only layout | Phase 2 shell must keep stable regions for virtual renderer | `viewport` | Viewport halo may update | No canonical history | No file effect | No file effect | Desktop/mobile resize, no overlap | Inventoried | Keep cards out of editor shell. |
| LIB-001 | Library | Same Diagram document list | Diagram screen cards/tree | `loadDiagramDocuments`, `select-diagram-document`, `select-diagram-card` | Reads Documentation/Diagram rows and document image state | Existing Diagram 2 document loader stays shared; Phase 7 hardens flows | `none` | None | Selection history is route/UI only | Same document IDs | Same document IDs | Large list selection without renderer leak | Inventoried | Already shares `diagram-documents.js`. |
| LIB-002 | Library | Card/tree view toggle and left pane | Header/menu buttons | `set-diagram-view`, `toggle-diagram-tree-pane` | Reads/writes user preferences only | Existing Diagram 2 preference keys remain separate | `none` | None | No canonical history | No file effect | No file effect | Toggle without canvas rerender | Inventoried | Keep `pmt-diagram2-*` keys isolated. |
| LIB-003 | Library | Filters and sort | Diagram filters dialog/search row | `openDiagramFiltersDialog`, search/project/sprint/visibility/sort | Reads document metadata; writes filter UI preferences | Phase 7 mirrors Diagram 1 filter dialog if editor parity needs it | `none` | None | No canonical history | Same docs visible | Same docs visible | Filter large lists only | Inventoried | Diagram 2 currently has inline filters, not full Diagram 1 filter dialog. |
| DOC-001 | Document | New Diagram | Header `New Diagram` | `createNewDiagram`, `createDiagramDocument`, editor save | Writes new empty canonical SVG/state and metadata | Phase 7 adds Diagram 2 creation flow after editor shell exists | `document` | Initial render only | `create-document` boundary | D1 can open new D2 document | D2 can open D1 new document | New/open/save under small fixture | Inventoried | Must create normal Diagram backing document, not Diagram 2 copy. |
| DOC-002 | Document | Edit Diagram Info | Tree context/page action | `editDiagramInfo`, `saveDiagramInfo` | Reads/writes title, visibility, project, sprint, parent, row version | Phase 7 reuses existing metadata endpoint/dialog contract | `document-metadata` | None | `edit-document-info` | Metadata preserved | Metadata preserved | No renderer work | Inventoried | Row-version conflict behavior must remain unchanged. |
| DOC-003 | Document | Duplicate | Tree context menu | `duplicateDiagram`, `createDiagramDocument` | Reads canonical SVG/state; writes cloned document body and metadata | Phase 7 duplicates through same backing service | `document` | Initial render after open | `duplicate-document` boundary | D1 clone opens in D2 | D2 clone opens in D1 | Clone 232 fixture without cache persistence | Inventoried | D2 renderer caches must never be cloned. |
| DOC-004 | Document | Delete | Tree context menu | `deleteItem` | Deletes Diagram backing document record | Phase 7 reuses existing delete confirmation path | `document` | Destroy renderer if current doc deleted | No editor undo | Removed doc absent both screens | Removed doc absent both screens | Destroy cleanup on delete | Inventoried | Preserve permissions and confirmation copy. |
| DOC-005 | Document | Public Link | Page/tree action | public-link handler | Reads document visibility/id | Phase 7 mirrors link behavior; no editor renderer work | `none` | None | No canonical history | Same URL target | Same URL target | No renderer work | Inventoried | Requires same route/link semantics as Diagram 1. |
| VIEW-001 | Read-only | Read-only zoom controls | Page document actions | `zoom out`, `select zoom`, `zoom in`, `fit` | Reads canonical state and viewport; writes viewport transform only | Existing Diagram 2 transform-only zoom stays baseline | `viewport` | Viewport halo/detail may update | Viewport ops not undoable | N/A | N/A | 29, 232, 500 transform-only, fullRenderCount unchanged | Inventoried | Already partially covered by Diagram 2 tests. |
| VIEW-002 | Read-only | Read-only context menu | Read-only SVG area | toggle relationships, mapping, copy SVG/PNG | Reads visibility flags, selected field mapping state | Phase 7 mirrors menus after editor commands are stable | `view-settings`, `clipboard` | Relationship visibility only | No canonical history except if setting persisted later | Copy output parseable | Copy output parseable | Context menu does not rebuild full SVG | Inventoried | Diagram 1 context includes relationship and field mapping display toggles. |
| IO-001 | Import/export | PMT Diagram import | Hidden file input/header | `importPmtDiagramFile`, `parsePmtDiagramFile`, `createDiagramDocument` | Reads `.pmt-diagram`; writes backing Diagram document canonical SVG/state | Phase 7 uses shared `diagram-contracts.js` parser | `document` | Initial render after import | `import-document` boundary | D1 files import to D2 | D2 files import to D1 | Import 232/500 without renderer cache fields | Inventoried | Format remains `pmt-diagram` v1. |
| IO-002 | Import/export | PMT JSON export | Tree/header export | `exportPmtDiagram`, `createPmtDiagramFile` | Reads normalized canonical state and metadata; writes file only | Phase 7 exports from canonical state, not live DOM | `none` | None | No canonical history | D1 export opens D2 | D2 export opens D1 | Export large file without stringifying caches | Inventoried | Current Diagram 2 has early PMT export action. |
| IO-003 | Import/export | SVG export/download | Tree/page/header | `buildAnnotationSvg`, download SVG | Reads canonical state and embedded assets | Phase 7 keeps shared SVG builder until renderer-safe writer exists | `none` | None | No canonical history | SVG parseable by D2 | SVG parseable by D1 | SVG build timed for 232/500 | Inventoried | Export is allowed to build SVG once; routine edits are not. |
| IO-004 | Import/export | PNG export/copy | Tree/page/header | `copyAnnotationPngToClipboard`, PNG export | Reads SVG/canvas projection | Phase 7 creates off-screen export path separate from live renderer | `none` | None | No canonical history | PNG reflects D1 state | PNG reflects D2 state | Off-screen export memory cleanup | Inventoried | Export may be slower than viewport, but must not poison renderer state. |
| IO-005 | Import/export | Portable embedded assets | PMT/SVG import/export | portable state helpers, embedded image upload | Reads/writes image URLs, embedded data, crop metadata | Phase 6/7 reuse shared asset normalization | `asset` | Object bounds may change after image load/crop | `insert-image`, `update-image` | Assets survive D1 to D2 | Assets survive D2 to D1 | Large embedded image export cleanup | Inventoried | No second asset store. |
| TOOL-001 | Tool | Select | Toolbar and `V` shortcut | `set activeTool=select`, pointer handlers | Reads objects/relationships; writes selection ids | Phase 3 controller command and renderer selection API | `selection` | None | No canonical history | Selection package parseable | Selection package parseable | Click/multi-select under 232 | Performance Complete | Default edit tool and cursor; shared across top-navigation and RTE hosts. |
| TOOL-002 | Tool | Pan | Toolbar and `H` shortcut | `activeTool=pan`, pan pointer handlers | Reads viewport transform; writes viewport transform only | Existing transform-only pan remains; Phase 3 integrates with editor tool state | `viewport` | Viewport halo/detail only | No canonical history | No file effect | No file effect | Pan frame, no full render | Performance Complete | |
| TOOL-003 | Tool | Format Painter | Toolbar | format painter state/apply | Reads source object style; writes target object style | Phase 3 command copies style through command bus | `style` | Zero relationship reroutes for style-only application | `apply-format-painter` | Style preserved | Style preserved | Batch apply patches only targets | Performance Complete | Compatible style fields apply to one or multiple targets; Escape resets the tool. |
| TOOL-004 | Tool | Crop | Toolbar and `C` shortcut | crop gesture, crop inspector | Reads image/canvas crop; writes crop rect, insets, radius | Phase 6 implements image crop commands | `geometry`, `asset` | Bounds/halo update for cropped object | `set-crop`, `reset-crop`, `permanent-crop` | Crop metadata preserved | Crop metadata preserved | Crop preview no full render | Inventoried | Crop tab appears only when image/canvas supports it. |
| TOOL-005 | Tool | Grid toggle | Toolbar | grid checkbox/toggle | Reads/writes canonical grid flag | Phase 3 canvas-option command and renderer grid plane | `view-settings` | None | `canvas-option` with inverse | Opens | Opens | Toggle no full render | History Complete | |
| TOOL-006 | Tool | Snap toggle | Toolbar | snap checkbox/toggle | Reads/writes canonical snap flag | Phase 3 tool option used by move/resize/draw commands | `none` | None | `canvas-option` with inverse | Opens | Opens | Move/resize snap under 232 | History Complete | |
| TOOL-007 | Tool | Delete | Toolbar and Delete/Backspace | `handleAnnotationAction('delete')` | Removes selected unlocked core objects | Phase 3 batch delete command removes by indexed ids | `structure` | Existing affected relationships update through renderer removal | `delete-selection` | Deleted output opens | Deleted output opens | Batch delete keeps unrelated nodes | Performance Complete | Protected and locked objects are not deleted. |
| TOOL-008 | Tool | Undo/redo | Toolbar and Ctrl+Z/Ctrl+Y | `handleAnnotationAction('undo'/'redo')` | Applies command inverse/redo | Phase 2/3 bounded command history and indexed canonical updates | Per command | Per command | `undo`, `redo` stack | Final state compatible | Final state compatible | 1,200-object controller and 238-object browser fixtures | Performance Complete | Full snapshot restore remains reserved for global replacement boundaries. |
| OBJ-001 | Object | Rectangle | Toolbar and `R` shortcut | rectangle drawing gesture | Writes object `{type:'rectangle', bounds, style}` | Phase 3 draw command and incremental mount | `object-add`, `geometry`, `style` | Obstacle index add | `add-rectangle` | Shape opens | Shape opens | Add/move/resize no full render | Performance Complete | Completed in Phase 3; Phase 4 adds drawing-default reuse for rectangle style. |
| OBJ-002 | Object | Circle | Toolbar and `O` shortcut | circle drawing gesture | Writes object `{type:'circle', bounds, style}` | Phase 3 draw command | `object-add`, `geometry`, `style` | Obstacle index add | `add-circle` | Shape opens | Shape opens | Same as rectangle | Performance Complete | Completed in Phase 3. |
| OBJ-003 | Object | Arrow | Toolbar and `A` shortcut | arrow drawing, endpoint adjustment | Writes arrow geometry, style, heads/symbols | Phase 3 basic annotation arrows; Phase 5 relationship-aware arrows | `object-add`, `geometry`, `style` | Endpoint/obstacle routing if entity-linked | `add-arrow`, `move-arrow-endpoint` | Arrow opens | Arrow opens | Endpoint move patches one object | Performance Complete | Annotation arrows completed in Phase 3; ERD relationships remain Phase 5. Phase 4 adds drawing-default reuse for arrow style. |
| OBJ-004 | Object | Line | Toolbar and `L` shortcut | line drawing | Writes line endpoints/style | Phase 3 draw command | `object-add`, `geometry`, `style` | None unless obstacle | `add-line` | Line opens | Line opens | Same as arrow | Performance Complete | Completed in Phase 3. |
| OBJ-005 | Object | Text Box | Toolbar and `T` shortcut | text box drawing/edit | Writes text, bounds, text style | Phase 3 text command and editor overlay | `object-add`, `text`, `geometry`, `style` | Bounds index update | `add-text`, `edit-text` | Text opens | Text opens | Inline edit no full render | Performance Complete | Completed in Phase 3. |
| OBJ-006 | Object | Rich Text | Toolbar and `Y` shortcut | rich text creation/editor | Writes rich text HTML/content metadata and bounds | Phase 3 rich-text command and shared RTE dialog | `object-add`, `text`, `asset` | Bounds index update | `add-rich-text`, `edit-rich-text` | Rich text opens | Rich text opens | Large content edit bounded | Performance Complete | Completed in Phase 3 with origin-clean PNG output preserved. |
| OBJ-007 | Object | Image | Paste/upload/import path | image insertion/upload handlers | Writes image URL/data, dimensions, crop metadata | Phase 6 image command and asset adapter | `object-add`, `asset`, `geometry` | Bounds index update | `insert-image` | Image opens | Image opens | Decode/load cleanup | Inventoried | |
| OBJ-008 | Object | Entity | Toolbar and `E` shortcut | entity creation/parsing handlers | Writes entity object, fields, style, relationships | Phase 5 entity command set | `entity`, `structure`, `relationship` | Adds obstacles/routes | `add-entity`, `edit-entity` | Entity opens | Entity opens | 232/500 add/edit local reroute | Inventoried | |
| OBJ-009 | Object | Field Rectangle | Toolbar | field rectangle create/connect | Writes field rectangle object and mapping/connection metadata | Phase 6 mapping object command | `object-add`, `mapping`, `geometry` | May link to entity field bounds | `add-field-rectangle`, `connect-field-rectangle` | Opens | Opens | Mapping hover large doc | Inventoried | |
| OBJ-010 | Object | Field Mapping Table | Toolbar/action | generate table handler | Writes mapping table object, rows, styles | Phase 6 table command and exporter hooks | `object-add`, `mapping`, `style` | Bounds index update | `generate-field-mapping-table` | Table opens | Table opens | Large table mount virtualized if needed | Inventoried | |
| SEL-001 | Selection | Click select | SVG objects | pointer handlers | Reads hit target; writes selected ids | Shared Phase 3 interaction engine and renderer selection API | `selection` | None | No canonical history | Selection copied via shared package | Selection copied via shared package | 238-object click under frame budget | Performance Complete | Locked objects remain selectable but cannot mutate. |
| SEL-002 | Selection | Modifier multi-select | SVG objects | Ctrl/Command/Shift select behavior | Reads current selection; writes selected ids | Shared Phase 3 selection reducer | `selection` | None | No canonical history | Selection package supports ids | Selection package supports ids | Multi-select browser coverage | Performance Complete | |
| SEL-003 | Selection | Marquee | Canvas drag area | marquee gesture | Reads indexed object bounds; writes selected ids | Phase 3 renderer spatial index/hit test API | `selection` | None | No canonical history | Selection ids parseable | Selection ids parseable | Marquee p95 at or below 0.3 ms in stress gate | Performance Complete | Client-coordinate origin correction matches the cursor. |
| SEL-004 | Selection | Select All | Ctrl+A | keydown handler | Reads canonical visible objects; writes selected ids | Phase 3 controller selection command | `selection` | None | No canonical history | Selection copied | Selection copied | Select All does not require mounted DOM nodes | Performance Complete | |
| GEO-001 | Geometry | Drag | Pointer drag | move handlers, geometry preview | Writes object bounds/points once on commit | Phase 3 command uses preview/commit renderer APIs | `geometry` | Connected/spatially affected routes only on commit | `move-objects` | Moved state opens | Moved state opens | Drag p95 below 24 ms in 238-object fixture | Performance Complete | Preview hides stale relationship routes and ends connectors at Entity borders. |
| GEO-002 | Geometry | Multi-drag | Multi-selection drag | move handlers | Writes multiple bounds/points once on commit | Phase 3 batch command | `geometry-batch` | Consider affected relationships only | `move-objects` batch | Opens | Opens | Batch move keeps unrelated node identity | Performance Complete | |
| GEO-003 | Geometry | Corner resize | Resize handles | resize handlers | Writes proportional bounds, dimensions, and points | Phase 3 resize command and preview API | `geometry` | Bounds/route indexes update on commit | `resize-objects` | Opens | Opens | Resize p95 below 21 ms in 238-object fixture | Performance Complete | |
| GEO-004 | Geometry | Side resize | Resize handles | resize handlers | Writes axis-specific bounds | Phase 3 shared resize command | `geometry` | Same as corner resize | `resize-objects` | Opens | Opens | Browser coverage across supported types | Performance Complete | |
| GEO-005 | Geometry | Keyboard nudge | Arrow keys, toolbar arrows | keydown/nudge handlers | Writes selected bounds/points | Phase 3 indexed command with Grid/Snap step | `geometry` | Local relationship reroute | `move-objects` with coalescing | Opens | Opens | Repeated nudge history coalescing | Performance Complete | |
| GEO-006 | Geometry | Fit arrow to head / endpoint helpers | Arrow controls and automatic helpers | endpoint resize and arrow rendering | Writes arrow endpoints/head geometry | Phase 3 resize preview/commit path | `geometry` | None unless linked | `resize-objects` | Opens | Opens | Arrow/line browser and unit coverage | History Complete | |
| STR-001 | Structure | Duplicate selection | Keyboard/context/action | duplicate handlers | Writes cloned objects with new ids and refs | Phase 3 core clipboard clone command with ID remap | `structure`, `object-add` | Clone selected compatible relationships | `duplicate-selection` | Opens | Opens | Batch clone no full render | Performance Complete | One history entry and repeated offset. |
| STR-002 | Structure | Group | Ctrl+G/action | group handler | Writes group membership/layer order and group metadata | Phase 4 group command | `structure` | Group bounds/index update | `group-selection` | Opens | Opens | Group large selection | Performance Complete | Shared by top-navigation Diagram 2 and both RTE Diagram 2 hosts. |
| STR-003 | Structure | Ungroup | Ctrl+Shift+G/action | ungroup handler | Removes group membership, preserves children/layers | Phase 4 ungroup command | `structure` | Bounds index update | `ungroup-selection` | Opens | Opens | Group roundtrip | Performance Complete | Shared by top-navigation Diagram 2 and both RTE Diagram 2 hosts. |
| STR-004 | Structure | Lock/unlock | Object context and Objects pane | lock action and object-tree lock icon | Writes lock flags | Phase 3 selected-object patch command; Phase 4 adds object/group tree lock toggles | `structure` | None | `set-lock` | Opens | Opens | No renderer rebuild | Performance Complete | Objects-tree lock/unlock uses the same patch command path as selected-object locking. |
| STR-005 | Structure | Visibility/show-hide | Objects pane/action | visibility action | Writes object/group visibility flags | Phase 4 property command | `visibility` | Hidden obstacles/relationships handled consistently | `set-visibility` | Opens | Opens | Hide group no full render | Performance Complete | Group rows and object rows share one structure command. |
| STR-006 | Structure | Rename | Objects pane | rename action | Writes object display name and group-name metadata | Phase 4 property command | `metadata` | None unless label bounds change | `rename-object` | Opens | Opens | Text update patches only target | Performance Complete | Group and object rename are command-based and undoable. |
| STR-007 | Structure | Bring front/back/forward/backward | Object context and Layers toolbar | layer order handlers | Writes canonical object order | Phase 3 selected-object order command extended with Phase 4 group-aware z-order and Layers UI | `structure` | Hit testing order changes; routing unchanged | `arrange-objects` | Opens | Opens | Mounted SVG nodes reorder with no full render | Performance Complete | Phase 4 completes the full Layers UI and group-aware z-order behavior. |
| STR-008 | Structure | Object tree search/filter | Objects pane | `buildAnnotationObjectTree`, filter handlers | Reads object tree projection only | Phase 4 tree projection helper over shared Diagram 1 tree helpers | `none` | None | No canonical history | No file effect | No file effect | 1,000-object tree search browser and unit evidence | Performance Complete | Search is UI-only state and is not serialized. |
| STR-009 | Structure | Object tree drag/drop reorder | Objects pane tree | `reorderAnnotationObjectTree` | Writes layer/order/group membership | Phase 4 command over tree drop target | `structure` | Hit testing order and group bounds | `tree-reorder` | Opens | Opens | 1,000-object tree reorder undo/redo no full render | Performance Complete | Reorder preserves command history and compact group layers. |
| STYLE-001 | Style | Fill color, transparent fill | Format tab | color split-button/toggles | Writes object fill/transparent flags | Phase 3 style command | `style` | None | `set-style` | Opens | Opens | Batch style patch selected ids | Performance Complete | D1 position, split action, and remembered/recent colors are shared. |
| STYLE-002 | Style | Outline color/show outline | Format tab | color/toggle inputs | Writes stroke visibility/color | Phase 3 style command | `style` | Zero relationship reroutes for core style-only changes | `set-style` | Opens | Opens | Patch targets only | Performance Complete | |
| STYLE-003 | Style | Opacity and line width | Format tab | slider/input | Writes opacity/stroke width | Phase 3 style command | `style` | Core object style patch only | `set-style` | Opens | Opens | Coalesced batch update, no full render | Performance Complete | |
| STYLE-004 | Style | Arrow heads and relationship symbols | Format/Entity tabs | arrow head/symbol controls | Writes arrow style metadata | Phase 3 for arrows; Phase 5 for ERD relationships | `style` | Core arrow redraw only; relationship style patch reroutes zero relationships | `set-style`, `set-relationship-style` | Opens | Opens | Selected arrow or selected relationship patch only | Performance Complete | Phase 5 completes ERD relationship symbols through shared global and individual relationship style commands. |
| STYLE-005 | Style | Text color/font/size/alignment | Format tab | text style controls | Writes text style and alignment | Phase 3 text style command | `style`, `text` | No route change for fixed-bounds text styling | `set-style` | Opens | Opens | Selected text patches under stress fixture | Performance Complete | |
| STYLE-006 | Style | Text content area | Double-click Text Box/Rich Text dialog | shared text and rich-text editors | Writes normalized text/rich-text content on commit | Phase 3 text command with dialog commit boundary | `text` | Zero relationship reroutes for content-only fixed-bounds changes | `edit-text` | Opens | Opens | Full D1 RTE controls; no per-keystroke serialization | Performance Complete | |
| ERD-001 | Entity | Create Entity | Toolbar and `E` shortcut | entity tool and parser dialog | Writes entity object, fields, style defaults | Phase 5 `diagram2-editor-entities` command plan via shared controller | `entity`, `object-add` | Obstacle and routing index add | `add-entity` | Opens | Opens | Browser top-nav/RTE creation plus controller undo/redo | Compatibility Complete | Entity creation is shared by top-navigation Diagram 2 and RTE hosts. |
| ERD-002 | Entity | SQL parsing | Entity dialog/import | `parseDiagram2EntityDefinition` wrapping `parseAnnotationEntityDefinition` | Reads SQL text; writes entity schema/fields | Renderer-neutral parser helper reused from Diagram 1 | `entity` | Relationships may be inferred/changed by compatible canonical foreign keys | `parse-entity-sql` | Opens | Opens | Parser and controller tests | Compatibility Complete | SQL Server `CREATE TABLE` parsing stays compatible with Diagram 1 canonical Entity fields. |
| ERD-003 | Entity | Field-list parsing | Entity dialog | shared parser helpers | Writes field definitions/order | Phase 5 Entity definition command | `entity` | Field bounds change relationship endpoints | `set-entity-fields` | Opens | Opens | Controller and browser dialog evidence | History Complete | Field-list reorder is supported by editing the definition text and committing one command. |
| ERD-004 | Entity | Edit schema/name/header style and reset scale | Entity inspector/dialog | Entity definition dialog, Reset Scale, plus Format/Entity color controls | Writes entity schema, name, natural bounds, header/text colors | Phase 5 Entity definition, natural-size, and shared style commands | `entity`, `style`, `geometry` | Bounds/route endpoints may update | `update-entity`, `reset-entity-scale`, `set-style` | Opens | Opens | Entity inspector screenshots and command tests | History Complete | Reset Scale recomputes natural dimensions from fields/display settings for the selected unlocked Entity. |
| ERD-005 | Entity | Add/remove/reorder fields | Entity dialog and inspector field rows | field parser plus add/update/remove/reorder helpers | Writes `fields[]` and order | Phase 5 command helpers in `diagram2-editor-entities` | `entity`, `geometry`, `relationship` | Field anchors and connected routes update | `edit-entity-fields`, `add-entity-field`, `remove-entity-field`, `move-entity-field` | Opens | Opens | Focused controller tests plus top-navigation/RTE browser field-row coverage | History Complete / Browser Complete | Visible inspector rows support add, edit, reorder, delete, duplicate suffixing, and relationship-safe cleanup. |
| ERD-006 | Entity | PK/FK/identity/nullable/important flags | Inspector field rows and Entity dialog | field property parser/toggles/reference dropdowns | Writes field metadata and compatible FK records | Phase 5 field command helpers | `entity`, `style`, `relationship` | FK mappings/relationship style can change | `set-field-flags`, `set-field-reference` | Opens | Opens | Focused controller tests plus browser reference dropdown coverage | History Complete / Browser Complete | Row-level flags and target Entity/field references flow through shared commands in both hosts. |
| ERD-007 | Entity | Collapse/expand | Entity controls | `setAnnotationEntityCollapsedState` | Writes collapsed flag | Phase 5 Entity option command | `entity`, `geometry`, `relationship` | Entity bounds and routes update | `set-entity-collapsed` | Opens | Opens | Browser Entity inspector toggle evidence | Renderer Complete | |
| ERD-008 | Entity | Show data types/key column | Entity controls | `setAnnotationEntityDataTypeVisibility`, key column toggle | Writes display flags | Phase 5 Entity option command | `entity`, `geometry`, `style` | Field row bounds may change | `set-entity-display-options` | Opens | Opens | Browser Entity inspector and RTE evidence | Renderer Complete | |
| ERD-009 | Entity | FK at top/alphabetize fields | Entity controls/dialog | parser/order helpers | Writes reordered fields | Phase 5 Entity option and definition command | `entity`, `relationship` | Field anchors and routes update | `reorder-entity-fields` | Opens | Opens | Controller parser evidence | History Complete | FK-at-top is exposed; alphabetize remains a Diagram 1 parser/order behavior rather than a separate Diagram 2 button. |
| ERD-010 | Entity | Anchor/allow overlap/manual relationship lines/self relationships | Entity and relationship controls | toggles | Writes entity/relationship routing preferences | Phase 5 routing command with renderer route flags | `relationship`, `routing` | Directly affects routing | `set-routing-options` | Opens | Opens | Browser manual-route, self-route, and selective-routing evidence | Performance Complete / Browser Complete | Self routes render only when enabled and remain selectable/editable. Compact refuses any locked Entity instead of treating it as an anchor; genuine anchor behavior matches D1. |
| ERD-011 | Relationship | Create/delete/select relationship | Relationship dialog, canvas hit path, Objects tree, Delete action | relationship helpers and stable hit paths | Writes relationships array and exact source/target field endpoints | Phase 5 relationship command and shared selection model | `relationship`, `selection` | Add/remove route and sector entries | `add-relationship`, `delete-relationship`, selection only | Opens | Opens | Browser and controller tests | Compatibility Complete / Browser Complete | Relationships are selectable from canvas or Objects rows using stable IDs and expose route joints when selected. |
| ERD-012 | Relationship | Style/symbols/global visibility | Entity inspector/read-only-compatible model | relationship style helpers | Writes relationship group style/display flags and individual overrides | Phase 5 relationship style command | `relationship`, `style` | Style-only changes reroute zero relationships | `set-relationship-style` | Opens | Opens | Controller assertions, D1 route parity, and browser inspector evidence | Performance Complete / D1 Parity Complete | Supports stroke, stroke width, opacity, arrow size, global defaults, individual overrides, and D1-directed one-to-one/one-to-many/many-to-one symbols. |
| ERD-013 | Relationship | Manual routes | Selected relationship route joints and Entity controls | D1 route geometry, insert/remove/adjust point, clear overrides | Writes route override points | Phase 5 command and renderer route-handle API | `routing` | Direct route override patch | `set-manual-route`, `add-route-point`, `remove-route-point`, `adjust-manual-route`, `clear-manual-routes` | Opens | Opens | Browser route-preview, double-click/right-click, drag, and controller undo/redo evidence | Performance Complete / Browser Complete | Selected routes show D1-style joints; double-click adds, right-click removes, drag previews D1 geometry, and each action commits once. |
| ERD-014 | Layout | Auto Format - Compact/org tree | Entity controls | exact shared D1 state operation, worker/inline engine, progress overlay | Writes D1-compatible Entity bounds and a signature-validated automatic-route snapshot | Phase 5 explicit D1-oracle Compact command with stale-result guard | `geometry-batch`, `routing` | Broad exact D1 reroute but no full SVG rebuild | `auto-format-entities` | Opens | Opens | 22-fixture D1/D2/worker oracle, exact route points, routes-only repair, save/read/reload parity, controller cancel/stale/history tests, permanent screenshot pairs | D1 Parity Complete / History Complete / Persistence Complete / Hardening Complete | Requires exactly one selected Entity, at least two Entities, and all Entities unlocked. Worker equals inline equals D1. The progress bar remains visible while exact routes are finalized. Compact commits a missing/stale route snapshot even when no Entity moves; valid unchanged output, cancel, and stale results commit nothing. |
| ERD-015 | Schema | Generate PMT Database Schema | Entity inspector | live schema API plus `buildPmtDatabaseSchemaDiagram` | Reads live schema metadata; writes compatible Documentation-backed Diagram | Phase 5 uses the existing schema builder and backing-document create path | `entity-batch`, `relationship-batch`, `document` | Broad route/index creation on generated Diagram open | `generate-pmt-schema-diagram` | Opens | Opens | PMT schema fixture browser/unit evidence plus live-generator Playwright coverage | Compatibility Complete / Browser Complete | Diagram 2 now exposes the live schema-generator button and preserves the existing fixture/import evidence. |
| MAP-001 | Mapping | Entity annotations | Entity inspector | `setAnnotationEntityAnnotation`, sync arrows | Writes annotation text/links and annotation arrows | Phase 6 annotation command | `mapping`, `text`, `relationship` | Annotation arrow routes update | `set-entity-annotation` | Opens | Opens | Owner/child/group indexes and dual-host browser evidence | Compatibility Complete / History Complete | Callout and arrow remain grouped to the owning Entity and move through indexed owner references. |
| MAP-002 | Mapping | Field Rectangle create/edit | Toolbar/Mapping controls | field rectangle handlers | Writes field rectangle object, label, target ids | Phase 6 command | `mapping`, `geometry` | Bounds/field links update | `edit-field-rectangle` | Opens | Opens | Local command/renderer tests and dual-host browser evidence | Compatibility Complete / History Complete | Field Rectangles remain D1-compatible virtual `type: "entity"` objects and show only Field Rectangle controls in the inspector. |
| MAP-003 | Mapping | Map UI field to DB field | Entity/Mapping inspector | `setAnnotationEntityFieldForeignKeyMapping`, mapping target helpers | Writes mapping relationships, target field ids | Phase 6 command | `mapping`, `relationship` | Highlight/field route only | `set-field-mapping` | Opens | Opens | 500/1,000 local mapping gates | Performance Complete / History Complete | Mapping commits one command, renames the Field Rectangle to the database field, and updates only affected indexes/routes/table rows. |
| MAP-004 | Mapping | Many-to-one mappings | Mapping state/model | mapping helpers | Writes arrays of UI-to-DB mapping records | Phase 6 command and validation | `mapping` | Field highlight routes update | `set-field-mappings` | Opens | Opens | Multiple stable mapping IDs and row keys | Compatibility Complete | Simple, one-to-one, one-to-many, and many-to-one metadata remains D1-readable. |
| MAP-005 | Mapping | Mapping hover/highlight | Read-only/editor | hover state and highlight SVG helpers | Reads mapping state; writes hover UI only | Phase 6 renderer overlay, no persistence | `selection`, `view-settings` | Highlight overlay only | No canonical history | No file effect | No file effect | Hover at 500/1,000 without full render or broad scan | Performance Complete / Browser Complete | Keyed highlight targets cover source Field Rectangle, database Entity field, table row, and attention routes. |
| MAP-006 | Mapping | Generate field-to-entity mapping | Entity controls | generator handlers | Writes mapping records/field rectangles/table | Phase 6 batch command | `mapping-batch`, `object-add` | Adds overlays and routes | `generate-field-mapping` | Opens | Opens | Shared controller and browser generation evidence | History Complete / Renderer Complete | Field Mapping Table generation selects a source screenshot and creates one undoable compatible table. |
| MAP-007 | Mapping | Field Mapping Table colors/export | Mapping tab | shared style controls and standard Diagram export | Writes table style; export reads rows | Phase 6 table style plus standard export; Phase 7 owns dedicated tabular export hardening | `mapping`, `style` | None | `set-mapping-table-style` | Opens | Opens | Stable keyed rows and SVG/PNG/PMT round trip | Compatibility Complete / Renderer Complete | Header/UI/database colors and highlight styling persist; dedicated Excel/CSV work remains Phase 7. |
| MAP-008 | Mapping | Read-only field mapping interactions | Read-only context/toggles | field mapping selection/highlight SVG | Reads mapping data; writes view-only selection | Phase 6 renderer view state | `selection`, `view-settings` | Overlay only | No canonical history | No file effect | No file effect | Hover/click/double-click/keyboard smoke | Performance Complete / Browser Complete | Read-only mapping interaction does not mutate canonical state or trigger a full render. |
| IMG-001 | Image | Insert image/upload | Clipboard/upload/drop | shared Rich Text upload handlers | Writes image object URL/data/dimensions | Phase 6 image command and keyed resource adapter | `asset`, `object-add` | Bounds index add | `insert-image` | Opens | Opens | Upload/drop/paste, missing fallback, cache/release, dual-host browser smoke | Compatibility Complete / History Complete / Renderer Complete | Reuses the existing upload endpoint and canonical image contract. D1 vector opacity intentionally leaves images unchanged. |
| IMG-002 | Image | Crop inset/radius/reset | Crop inspector | nine numeric `input` controls, Crop handles, reset, visibility, permanent crop | Writes crop insets, uniform/independent corner radii, source data if permanent | Phase 6 shared 500 ms numeric scheduler, crop commands, and renderer-local transient preview/selection suppression | `asset`, `geometry` | Selected image and Crop overlay only; ordinary selection chrome suppressed for D1's 3,000 ms quiet period | `set-crop`, `reset-crop`, `permanent-crop` | Opens | Opens | No-Tab input, pointer/keyboard preview, flush/cancel transitions, undo/redo, save/reopen, top-navigation and RTE evidence | D1 Parity Complete / Performance Complete / History Complete / Browser Complete | Twenty inputs produce one image patch, one overlay patch, and one history command with zero full renders, repeated decodes, unrelated object patches, or relationship reroutes. Pointer preview remains immediate. Permanent crop clears undo because source replacement is irreversible, matching D1. |
| TPL-001 | Template | Apply template | Template inspector list | `instantiateAnnotationTemplate`, apply formatting | Reads template library; writes created objects/styles | Phase 4 template command uses shared template codec | `object-add`, `style`, `structure` | Adds obstacles/routes if entities | `apply-template` | D1 templates apply in D2 | D2 templates apply in D1 | Apply template no full render | Performance Complete | Template application is undoable document history; template formatting patches selected compatible objects. |
| TPL-002 | Template | Save selection as template | Template inspector | `captureAnnotationTemplate`, `portableAnnotationTemplate` | Reads selected objects; writes template library record | Phase 4 shared template serializer | `template` | None | `save-template` not document undo | Templates shared | Templates shared | Selection serialization smoke | Compatibility Complete | Uses the shared Diagram template endpoint and codec. |
| TPL-003 | Template | Upload/download/delete templates | Template inspector | upload/download/delete handlers | Reads/writes template library endpoint | Phase 4 reuse shared endpoints and parser | `template` | None | Template ops outside document undo | Shared endpoint | Shared endpoint | Endpoint compatibility tests | Compatibility Complete | Upload, download, delete, rename, update, and reorder are template-library operations, not Diagram document undo entries. |
| TPL-004 | Template | Restore default templates | Template inspector | `restoreAnnotationDefaultTemplates` | Writes template library from default endpoint | Phase 4 reuse existing endpoint | `template` | None | Template ops outside document undo | Shared endpoint | Shared endpoint | Endpoint compatibility tests | Compatibility Complete | Capacity handling mirrors the shared default-template helper. |
| TPL-005 | Template | Drawing defaults: use/reset selected arrow/rectangle | Template/default controls | default style handlers | Reads selected object style; writes local defaults/template prefs | Phase 4 controller defaults, no canonical write until drawing | `none` | None | No canonical history | Defaults not persisted to file | Defaults not persisted to file | Draw after default under no render | Performance Complete | Rectangle and arrow defaults are shared across top-navigation and RTE hosts; Field Rectangle defaults remain Phase 6. |
| CLIP-001 | Clipboard | Copy/paste same screen | Ctrl+C/Ctrl+V/action | shared copy/paste handlers | Reads selected canonical objects; writes remapped clones on paste | Phase 3 uses shared selection clipboard package | `clipboard`, `object-add` | Adds affected routes only when pasted content requires them | `paste-selection` | N/A | N/A | Repeated paste and batch history browser coverage | Performance Complete | Top-navigation and RTE hosts share the controller path. |
| CLIP-002 | Clipboard | Diagram 1 to Diagram 2 | Cross-screen clipboard | shared package parser/remap | Reads D1 selection package; writes D2 canonical objects | Shared v1 clipboard codec and same-tab fallback | `clipboard`, `object-add` | Adds affected routes | `paste-selection` | Required | Required output parseable | Unit and Diagram 1 browser regression tests | Compatibility Complete | |
| CLIP-003 | Clipboard | Diagram 2 to Diagram 1 | Cross-screen clipboard | shared package parser/remap | Reads D2 package; writes D1 objects on paste | D2 writes shared v1 package without renderer state | `clipboard`, `object-add` | Adds affected routes | `paste-selection` | Required output parseable | Required | Unit and Diagram 1 browser regression tests | Compatibility Complete | |
| HIST-001 | History | Command-based undo/redo | Toolbar/keyboard | Diagram 2 command history | Reads/writes command stack and inverse patches | Phase 2/3 coalesced command history with indexed canonical updates | Per command | Per command | Root history contract | Final state parseable | Final state parseable | 1,200-object controller fixture | Performance Complete | Full snapshot restore remains only for import/reset/open recovery. |
| HIST-002 | History | Coalescing continuous edits | Drag/resize/text/style inputs | gesture begin/commit, style merge window | Writes one history entry per user action | Phase 3 history transaction/merge behavior | Per command | Per command | Command merge/gesture boundary | Compatible final state | Compatible final state | Drag, resize, and rapid style tests | Performance Complete | Prevents history bloat. |
| KEY-001 | Keyboard | Tool shortcuts | Editor keydown | V,H,R,O,A,L,T,Y,E and tool Escape behavior | Writes active tool or creates a new object | Shared shortcut router | `none` or `object-add` for create shortcuts | None unless creating an object | No canonical history unless an object is created | No file effect or compatible new object | No file effect or compatible new object | Shortcut does not steal input typing | Performance Complete | Entity shortcut is complete; Crop remains Phase 6. |
| KEY-002 | Keyboard | Edit shortcuts | Editor keydown | Ctrl+Z/Y/C/V/D/A/G, Ctrl+Shift+G, Delete, arrows, route-handle arrows, Escape | Reads focus/selection; runs commands | Shared controller commands | Per command | Per command | Per command | Compatible output | Compatible output | Browser smoke with form/rich-text exclusion | Performance Complete | Relationship Delete and manual-route keyboard nudge are now included; Crop and mapping shortcuts remain in later phases. |
| CTX-001 | Context menu | Editor object context commands | SVG/editor area | shared action handlers | Reads selection and object capabilities; writes per action | Shared controller commands for object and relationship selection | Per command | Zero route work for arrange/lock/style-free copy; relationship delete removes only selected relationships | Per command | Compatible output | Compatible output | Context menu/actions retain keyed nodes and full-render count | Performance Complete | Arrange, group, ungroup, lock, clipboard, duplicate, delete, artwork copy, and relationship delete are complete; Crop/mapping actions remain excluded. |
| CTX-002 | Context menu | Diagram tree and canvas export commands | Library tree and viewer canvas | edit info, edit, duplicate, link, downloads, export, delete, canvas Copy as SVG/PNG | Reads document metadata/body; writes doc or clipboard output | Existing tree commands plus Phase 3 canvas-copy adapter; Phase 7 owns final workflow hardening | `document` or none | Destroy if current doc deleted | Document boundary ops | Same docs | Same docs | Tree menu and both-mode canvas-copy browser smoke | Compatibility Complete for current paths | Copy dialogs are shared with the existing download options dialog. |
| SAVE-001 | Persistence | Save same backing document | Apply/Save | `saveDiagramDocument`, `buildAnnotationSvg` | Reads canonical state; writes existing Diagram document body/row version | Existing D2 save persists canonical Compact route output but no live renderer cache | `document` | None during save | `save` clears dirty marker, not undo | D1 can open | D2 can open | Compact SVG round trip and live document 31 Save/close/reload | Compatibility Complete / Compact Persistence Complete | Compact route points and their geometry signature survive Save and are accepted only while the current Entity/relationship state matches. |
| SAVE-002 | Persistence | Row-version conflict | Save path | conflict handler offers Save as New | Reads row version; writes update or new doc | Phase 7 mirror Diagram 1 conflict UX | `document` | Initial render after save-as-new | Save boundary | Compatible | Compatible | Conflict smoke | Inventoried | |
| SAVE-003 | Persistence | Cancel/apply close semantics | Dialog footer | Cancel, Apply/Save buttons | Reads dirty state; writes saved document or discards session | Phase 7 final shell behavior | `document`, `lifecycle` | Destroy on close | Save/apply as command boundary | Compatible final state | Compatible final state | Close dirty prompt no leak | Inventoried | |
| PERF-001 | Performance | No routine full SVG rerender | All editor operations | renderer dirty flush APIs | Reads command dirty categories; writes live node patches | Phase 2-8 gate every command through dirty categories | All | Must be selective | N/A | Compatible final state | Compatible final state | FullRenderCount unchanged after open for routine ops | Performance Complete for Phase 5 scope | Export/save may build SVG off-screen; routine edit must not. |
| PERF-002 | Performance | Relationship spatial routing | Entity drag/resize/style/route changes | routing helpers/indexes | Reads obstacles, relationships, sectors; writes route paths | Phase 5/8 maintain route sectors and local invalidation | `routing` | Primary impact | Per relationship command | Routes preserved | Routes preserved | 232/500/1000 local reroute budgets | Performance Complete for Phase 5 functional gates | Browser evidence asserts selective 232/624 local routing and exercises 500/1000 move, style, and manual-route edits. Phase 8 retains sustained promotion budgets. |
| PERF-003 | Performance | Viewport halo/low-detail overview | Zoom/pan/fit | renderer viewport APIs | Reads transform/bounds; writes mounted detail level | Existing renderer baseline with Phase 5 Entity LOD correction; Phase 8 extends promotion gates | `viewport` | Halo filters affected routes | No canonical history | No file effect | No file effect | 232/500/1000 zoom/pan/fit | Performance Complete for Phase 5 functional gates | Low detail requires at least 80 Entities, enters below 6 projected row pixels, exits at 8, and conditionally keeps Entity name/PK/FK summaries readable. The 500/1000 gates verify virtualization and one initial full render with no routine increase. |
