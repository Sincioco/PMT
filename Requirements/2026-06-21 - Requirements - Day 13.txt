Project Management Tool (PMT):  Day 13 of Codex Agentic AI Coding
-----------------------------------------------------------------
Total Dev Time in Hours:  16

For all of this conversation / session, I will perform all the manual tests and compile the application manually.  Perform only the coding changes required and nothing else.

Dev Task Screen
---------------
1.) In the Dev Task Screen, change the Chart icon so it is a simple monochrome icon like the rest of the page-level icons/buttons.
2.) In the Dev Task Screen, when I click on a chart data point like the Sprint Dev Task Mix chart, and a dialog box shows up with a list of items to choose from, remove the last column which only has the view icon.  It is now redundant since clicking on the row views the item as well.
3.) In the Dev Task Screen, in the Table of tasks, the action buttons for each row, the buttons should not have a border unless the mouse cursor hovers on it.  Scale the icons a little bit bigger, the same size as the page-level icons.  Replace icons that have colors, such as the Delete and Audit log, and replace them with monochrome icons.
4.) Universally, update all other page titles and dialog box titles to use the same text color as the Dev Task screen's Page Title.
5.) In the Dev Task Screen, in the table, in each row, the project code and work item code should not be rendered in a blue-ish color because it implies to the user it is a hyperlink.  Make the color of this text the same text color as the Page's Title.  In "Paper Design" we only want text to have a color blue (or looks like the default hyperlink blue) if it is meant to be hyperlinkable.
6.) For the sub task, render the "sub-task" capsule in the same line as the project code and work item.  Attempt to make the sub-task rows the same height as a normal task rows.
7.) Make the "sub-task" capsule appear after the project code and work item.  Change the text color of the "sub-task" capsule to use the same text color as the Page's Title.  Proper case it so it reads "Subtask" (no dash).
8.) The class "subtask-pill" seems to be rendering the text as "SUBTASK".  Please fix that class or any class that is preventing the subtask to render as "Subtask" on-screen.
9.) When in read-mode, add more padding to the right of the % Completion column.  Currently, the graph appears too close to the right.

Dev Task Filter Dialog Box
---------------
1.) Make the Project and Sprint dropdown controls equal length and take up the width of the dialog box in a 2-column layout for the controls with appropriate padding on the left side, right side, and in the middle in between the two controls.
2.) Make the Sort dropdown have the same size as the Project dropdown (again that 2 column layout in the dialog box).
3.) Move the checkbox that reads "Hide Completed Dev Tasks" into the 2nd column of the 2nd row, to the right of the Sort dropdown.
4.) In the Status, Priority and Assigned Sections, make those proper case and create a virtual 4-column layout in each of those sections and move the checkboxes so each checkbox falls into their respective columns and warps to the next row.
5.) In the Assigned section, Render the person's avatar in between the checkbox and the name of the person.

6.) In the Dev Task Filter Dialog Box, widen the Project dropdown so it is the same width as the Sort dropdown.
7.) In the Dev Task Filter Dialog Box, render the checkbox for the "Hide Completed Dev Tasks" in the same line.  And if the text has to wrap, it should wrap not under the checkbox, but under the first word of the label.  Make this checkbox behavior a universal design of PMT as a part of the "Paper Design" UI principle.
8.) Change the dialog box "Done" button in "Apply Filter" but keep the checkmark icon.
9.) Update the close dialog box icon so that it is bigger, same size as the Dev Tasks page-level icons, and make the close icon not have a border unless the cursor hovers over it.  Make this a Universal change across all of PMT in both light and dark themes.  Again, make this a design principal of the "Paper Design" UI principle that PMT is using.

10.) In the Dev Task Filter Dialog Box, if the "All Projects" is selected in the Project dropdown and "All Sprints" is selected in the Sprints dropdown, then the Charts in the Dev Tasks screen should display "All Projects and All Sprints" in their subtitles.  This also applies to the "Dev Tasks Completed by Sprint" chart.  Just note the nuance that the "Dev Tasks Completed by Sprint" chart is designed to always show All Sprints, the filter changes whether it is by project or all projects.
11.) In the Dev Tasks Completed by Sprint chart, change the text that reads "All Sprints, latest first." into a dynamic text that shows the Project code then "All Sprints" if a project is selected.
12.) In the Dev Tasks Completed by Sprint chart, conform to the sub-title format of the other charts so they look uniform.
13.) Update the other charts so it conforms to this new format of "{Project Code} - {Sprint}".  Note the space before and after the dash (-).
14.) That last update duplicated the Project code in the subtitles of the other 3 charts.  Now it renders something like this: PMT - PMT-Sprint05

Dev Task Edit Mode
------------------
1.) When the Dev Task is in Edit mode, change the behavior of the table so that the user can drag-and-drop to re-order the tasks without needing to grab the re-order handler on the right.  This drag-and-drop logic only happens during edit mode.  And the cursor should update to the drag-and-drop cursor.
2.) Remove the re-order handler on the right, as that would be redundant now.
3.) In the dialog box where you view/edit a Dev Task, change the Title of the dialog box like in this format:  "PMT-TASK-001 - Build PMT foundation with stored procedures".
4.) In the dialog box where you view/edit a Dev Task, sometimes the close dialog box icon (the x at the upper-right) gets a blue focus.  Never allow the close button of the dialog box at the upper-right to ever get a focus.  Make this a universal change and part of the "Paper Design" principal to remember.
5.) When the Dev Task is in Edit mode, the % Complete graph has too much gap now between it and the icons to its right.  This was because we remove the drag handler and the graph has not been adjusted to take up more space.
6.) When the Dev Task is in Edit mode dialog box, double the gap between the left and right controls.  In the virtual 2-column layout, simply double the padding so the controls on the left is not so close to the control on the right.
7.) The Start field, which is a calendar field, has an incorrect height and appears much taller than a normal control.
8.) Below the description label, there are buttons for formatting.  Format those buttons to conform to "Paper Design".
9.) In the Assignee section, format the assignee into a 2-column layout.
10.) In the Assignee section, format the assignee into a 3-column layout.
11.) Style the checkboxes so they match the checkbox styling in the Dev Task Filter dialog.
12.) There is something wrong with the styling of the Checkboxes in the Add/Edit Dev Task dialog box specifically in the Assignees and Dependencies sections.  Please use the Dev Task Filters dialog box as a reference for what is the desired checkbox formatting, and fix the checkboxes in the Add/Edit Dev Task dialog box.  You have my permission to use a browser for visual inspection/comparison if that would help, but only for this task.

13.) When the Dev Task New/Edit dialog box is open, when I scroll the mousewheel to scroll up or down, when it reaches the end, the main browser's content on the back starts scrolling instead.  Can you fix it?  As part of the "Paper Design" principal, when a dialog box is open, the scrollwheel should operate only on the active dialog box and should not affect anything in the background.

Universal UI Change Theme Change
--------------------------------
1.) Universally in all buttons and dialog boxes across PMT both light and dark mode, remove the blue (primary class).  I don't want any buttons with a background.
2.) Universally, in all checkboxes, make sure that checkbox is vertically centered to the label that follows it (height-wise) and factoring the height of an image like an avatar if one is rendered before the label.  Currently, when a checkbox is rendered followed by an avatar and a label, the checkbox remains vertically aligned to the top.  The avatar image is rendered correctly which is vertically centered.
3.) Record in a separate file called "paper-design.md" in the docs that lists everything you remember about the "Paper Design" principle that we have been building.  Update the ui-design-system.md in docs and refer to the "paper-design.md" as the final authority in UI/UX behavior, meaning, it should supersede/override any previous UI principle.  And when in doubt, follow what is in the "paper-design.md" file.  And whenever I mention a new rule/logic/principle regarding "Paper Design" that is now recorded in the "paper-design.md" always add it in the future, remember this.

Bug Tracking Screen
-------------------
1.) Without breaking or touching anything in the Dev Task screen, mirror the Dev Task Screen onto the Bug Tracking Screen to include behavior, theme, UI/UX, chart behavior, table row behavior, etc. so that the two screens appear to share the same code sharing the same design (UI/UX) principles.
2.) A previous Codex session ran out of quota and did not finish, so I will prompt to re-execute the last instructions I gave to the previous Codex session:

Without breaking or touching anything in the Dev Task screen, mirror the Dev Task Screen onto the Bug Tracking Screen to include behavior, theme, UI/UX, chart behavior, table row behavior, etc. so that the two screens appear to share the same code sharing the same design (UI/UX) principles.

Verify the work and resume where the previous Codex sessoin left off.  I already noticed that the Drop Downs in the Bug Tracking Filters do not have the correct styling.

3.) In both the Bug Tracking Filters and Dev Tasks Filter, change the "Apply Filters" button at the bottom of the dialog box to "Done" but keep the icon.

You may use a browser to test/verify visually that the Dev Task and Bug Tracking screen looks uniformly the same.  But after this task is completed, do not perform any additional action other than code changes.  I will manually test and compile/build the project.

Top Navigation and Browser Scrollbar Change
-------------------------------------------
As I go from one screen to another in PMT, I notice the Top Nav reserves a space for the scrollbar on screens it is not needed.  Can you make it so that Top Nav always takes up the full width of the browser and the scrollbar, if needed, only appears below the Top Nav?


Dark Theme Fixes
----------------
* I Dark Theme, and only in Dark theme, when a button is selected or pressed, render it the original teal background it had before.  Currently, it is not rendering a background so it looks like nothing is selected.
* The background teal color you selected is pretty muted.  Pick the a style that's bright to indicate the button is selected.  And be sure the text is white when the button is selected.

PMT Logo
--------
* There is now a PMT logo stored in "D:\Project Management Tool (PMT)\Requirements\UI\PMT Logo.svg" use that and replace the current PMT logo.  Take a copy of it and store it where it should go for the web and do the appropriate scaling so the Top Nav stays at it is without anything shifting.  Just perform a logo swap.  And delete the old logo, I don't need it anymore.  And change the text "Software Engineering" into "Project Management Tool"
* Change the font size of the text "Project Management Tool" to font size 8pt;
* Put the text "PMT" just above the "Project Management Tool" and vertically center them against the height of the PMT logo.
* For the text "PMT", change the color of each letter as follows:  P=#4285F4, M=#EA4335, T=FBBC05

About Page
----------
* Create a brand new About page.
* Center the full PMT logo located in "D:\Project Management Tool (PMT)\Requirements\UI\PMT Logo - Full.svg"
* Add some small light brown text at the bottom that reads:
Created by:  Louiery R. Sincioco, June 2026, to help companies who cannot afford a Task/Project Management Tool have one for free.

* Make the background of PMT just white.  No gradients.  Just flat and white.

* Update the Logo subtitle "Project Management Tool" to use font-size: 11px;
* Update the about credit so that text color is just a dark light gray.  And make "Louiery R. Sincioco" a hyperlink to "http://sincioco.com".  And mention in the credits that the open-source and GitHub repository is located at "https://github.com/Sincioco/PMT"

* I meant for the light theme, universally, make the background of PMT white.
* Update the About credit text so that hyperlinks are rendered in a blue-ish text color (as per Paper Design).  Also, hyperlinks should be rendered with no underlines until the cursor hovers on it.  Add that in the "Paper Desing" principles and record it in the "paper-design.md" under the docs sub folder.

* Make PMT go to the About page if there is no Project specified in the system.
* In the About Page, nudge the PMT Logo on the page 20% higher.
* Nudge the logo 10% to the left.

* The avatar picture on the upper-right of the top nav has a double ring border.  Remove the inner-ring border and just have one outer ring border.